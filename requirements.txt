# IoT Replay Attack Defense Simulator - Dependencies
# =====================================================

# Core dependencies (for simulation)
# None required - uses stdlib only

# Visualization (for scripts/plot_results.py)
matplotlib>=3.5.0
numpy>=1.20.0

# API Server (for api.py)
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# Physical Experiment (for physical_experiment/)
pyzmq>=25.0.0
PyYAML>=6.0

# Testing
pytest>=7.0.0
httpx>=0.27.0

# Optional: Desktop GUI (uses tkinter from stdlib)
# Optional: Type checking
# mypy>=1.0.0
